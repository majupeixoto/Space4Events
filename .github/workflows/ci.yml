name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3  # Atualizado para v3 que usa Node.js 20

    - name: Set up Python
      uses: actions/setup-python@v4  # Atualizado para v4 que usa Node.js 20
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r Space4Events\Projeto\requirements.txt
      shell: cmd

    - name: Run Django migrations
      run: |
        cd Space4Events\Projeto
        python manage.py migrate
      shell: cmd

    - name: Run Django tests
      run: |
        cd Space4Events\Projeto
        python manage.py test
      shell: cmd

    - name: Set up Node.js
      uses: actions/setup-node@v3  # Atualizado para v3 que usa Node.js 20
      with:
        node-version: '20'

    - name: Install Cypress
      run: |
        cd Space4Events\Projeto
        npm install
      shell: cmd

    - name: Run Cypress tests
      run: |
        cd Space4Events\Projeto
        npx cypress run --config integrationFolder=Space4Events/Projeto/cypress/e2e
      shell: cmd
